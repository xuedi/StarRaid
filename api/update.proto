syntax = "proto3";

message UpdateRequest {
    string token = 1;
}

message UpdateResponse {
    message UpdateRequest {
        string token = 1;
    }
}

service UpdateService {
    rpc Search(UpdateRequest) returns (UpdateResponse);
}
