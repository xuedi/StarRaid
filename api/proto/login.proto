syntax = "proto3";
import "object.proto";

option go_package = "api/";

message LoginRequest {
  string username = 1;
  string password = 2;
}

message LoginResponse {
  string authToken = 1;
  repeated ObjectResponse objects = 2;
}

service LoginService {
  rpc Search(LoginRequest) returns (LoginResponse);
}
